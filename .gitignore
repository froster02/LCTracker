node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Extension build output
leetcode-srs-extension/dist/
leetcode-srs-extension/*.zip

# webapp/ has its own separate local git repo — not nested here
webapp/
